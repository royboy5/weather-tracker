'use strict';

export default class {
    constructor () {
        this.measurements = [];
    }
    
    addMetrics = measurements => {
        this.measurements.push(measurements);
    }
    
    
}